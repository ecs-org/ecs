Still to do
-----------

Templatetags:

 * feedback_summary should return the HTML snippet of comments for the
   specified object

Templates:

 * feedback_form should use AJAX to update feedback_summary (timed,
   and after submit)
 * create a separate includable template (or two) with the AJAX functions
   in it, to be included by the parent page

Tests:

 * Create fixtures with sample objects and feedback entries
 * tests to ensure that:
   * we can create new feedback entries
   * we can view feedback entries

...

psycopg2==2.4.1
pytz==2011n

# python docutils, needed by django, ecs, and others
roman==1.4.0
docutils==0.8.1

# django main
Django==1.2.3
South==0.7.2
django-piston==0.2.3
django-extensions==0.8
django-picklefield==0.1.9	# for docstash
django-dbtemplates==0.7.4
python-memcached==1.48		# for caching
mockcache==1.0.1		# for testing

# preprocessing, combining, compressing of js and css
cssmin==0.1.4
versiontools==1.8.2
django-appconf==0.4.1
django_compressor==1.1.1
pyScss==1.0.8

# queuing: celery
python-dateutil==1.5
anyjson==0.3.1
amqplib==1.0.2
kombu==1.5.1
pyparsing==1.5.6
ordereddict==1.1
celery==2.3.3
django-celery==2.3.3

# mail: ecsmail, communication: lamson mail server
chardet==1.0.1
Jinja2==2.6
lockfile==0.9.1
mock==0.7.2
python-daemon==1.5.5
lamson==1.1
BeautifulSoup==3.0.8
# from http://github.com/downloads/enki/beautifulcleaner/BeautifulCleaner-2.0dev.tar.gz
BeautifulCleaner==2.0dev

# search
Whoosh==2.3.2
# pysolr uses beautiful soup optional for solr error support
# pysolr uses httplib2 with fallback to httplib
httplib2==0.7.3
# we use == for pysolr because we dont want 2.1beta
pysolr==2.0.15
django-haystack==1.2.6

# excel generation / xlwt
xlwt==0.7.2

# (ecs/utils/pdfutils): pdf validation (is_valid, pages_nr)
pdfminer==20110515

# logging: django-sentry;
django-templatetag-sugar==0.1
django-indexer==0.3.0
django-paging==0.2.4
pygooglechart==0.3.0
django-sentry==1.11.4

# ecs.help needs reversion from now on
django-reversion==1.3.3

# django-rosetta is used only for doc.ecsdev.ep3.at , but we keep it in the main requirements for now
django-rosetta==0.6.2

# yaml is used for vm branding configuration
PyYAML==3.10

# browser detection
httpagentparser==1.7.0
